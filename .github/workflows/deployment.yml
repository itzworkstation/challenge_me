name: "Server Deployment werk"
on:
  push:
    branches: [develop]
jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Say Hello!
    run: echo "Hello there!d"
